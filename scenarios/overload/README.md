# Overload

Injects 50,000 records to create a sustained evaluation workload.

```bash
make demo-overload
```

Use Explorer and AutoTune to observe the workload. This is an evaluation scenario, not a production benchmark.
